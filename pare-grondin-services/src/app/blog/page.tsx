import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Blog — Health Sharing Resources for New Hampshire",
};

export default function BlogPage() {
  return <main>/* blog index — built in Stage 1E */</main>;
}
