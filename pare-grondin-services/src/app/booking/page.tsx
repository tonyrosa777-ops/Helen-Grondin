import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Booking",
};

export default function BookingPage() {
  return <main>/* booking page — built in Stage 1E */</main>;
}
